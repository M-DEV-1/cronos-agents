NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Agent Server URL (Optional, defaults to http://localhost:4000)
# Used by the Next.js API route proxy
AGENT_SERVER_URL=http://localhost:4000
# we'll change this to deployed URL